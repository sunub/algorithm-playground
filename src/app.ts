var uniquePathsWithObstacles = function (obstacleGrid: number[][]) {}

console.log(
    uniquePathsWithObstacles([
        [0, 0, 0],
        [0, 1, 0],
        [0, 0, 0],
    ])
)

const b = new Map()
const c = "Ha"

b.set("d", { [c]: 32 })
console.log(b)
